<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nachigo.pooltupi">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BarraLateral"
            android:label="@string/title_activity_barra_lateral"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Painel" />
        <activity android:name=".FAQ_activity" />
        <activity android:name=".Settings" />
        <activity android:name=".Sobre"></activity>
        <service android:name=".BlockFoundService"
            android:exported="false" />
    </application>

</manifest>